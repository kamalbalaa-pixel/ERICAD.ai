<UserControl x:Class="Frontend.Controls.CaptureControls"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             x:Name="Root">
    
    <StackPanel>
        <!-- Time Selection & Capture Buttons -->
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <!-- Time Selection Dropdown -->
            <ComboBox x:Name="TimeSelector" Grid.Column="0" Height="36" Margin="0,0,4,0"
                      IsEnabled="False" SelectedIndex="0"
                      Background="#333333" Foreground="White" BorderThickness="0">
                <ComboBox.Style>
                    <Style TargetType="ComboBox">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ComboBox">
                                    <Grid>
                                        <ToggleButton x:Name="ToggleButton" 
                                                      Background="#333333" BorderThickness="0"
                                                      IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                                      Focusable="False" ClickMode="Press">
                                            <ToggleButton.Template>
                                                <ControlTemplate TargetType="ToggleButton">
                                                    <Border x:Name="Border" Background="{TemplateBinding Background}" CornerRadius="6">
                                                        <Grid>
                                                            <Grid.ColumnDefinitions>
                                                                <ColumnDefinition Width="*"/>
                                                                <ColumnDefinition Width="28"/>
                                                            </Grid.ColumnDefinitions>
                                                            <ContentPresenter Grid.Column="0"/>
                                                            <Path Grid.Column="1" Data="M0,0 L4,4 L8,0" 
                                                                  Stroke="White" StrokeThickness="1.5"
                                                                  HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                        </Grid>
                                                    </Border>
                                                    <ControlTemplate.Triggers>
                                                        <Trigger Property="IsMouseOver" Value="True">
                                                            <Setter TargetName="Border" Property="Background" Value="#444444"/>
                                                        </Trigger>
                                                    </ControlTemplate.Triggers>
                                                </ControlTemplate>
                                            </ToggleButton.Template>
                                        </ToggleButton>
                                        <ContentPresenter x:Name="ContentSite" IsHitTestVisible="False"
                                                          Content="{TemplateBinding SelectionBoxItem}"
                                                          Margin="10,0,28,0"
                                                          VerticalAlignment="Center" HorizontalAlignment="Left"/>
                                        <Popup x:Name="Popup" Placement="Bottom" IsOpen="{TemplateBinding IsDropDownOpen}"
                                               AllowsTransparency="True" Focusable="False" PopupAnimation="Slide">
                                            <Border x:Name="DropDownBorder" Background="#222222" CornerRadius="6"
                                                    BorderBrush="#444444" BorderThickness="1" Margin="0,2,0,0"
                                                    MaxHeight="300">
                                                <ScrollViewer VerticalScrollBarVisibility="Auto">
                                                    <StackPanel IsItemsHost="True"/>
                                                </ScrollViewer>
                                            </Border>
                                        </Popup>
                                    </Grid>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                        <Setter Property="ItemContainerStyle">
                            <Setter.Value>
                                <Style TargetType="ComboBoxItem">
                                    <Setter Property="Background" Value="Transparent"/>
                                    <Setter Property="Foreground" Value="White"/>
                                    <Setter Property="Padding" Value="10,8"/>
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="ComboBoxItem">
                                                <Border x:Name="Border" Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}">
                                                    <ContentPresenter/>
                                                </Border>
                                                <ControlTemplate.Triggers>
                                                    <Trigger Property="IsHighlighted" Value="True">
                                                        <Setter TargetName="Border" Property="Background" Value="#FF6B35"/>
                                                    </Trigger>
                                                    <Trigger Property="IsSelected" Value="True">
                                                        <Setter TargetName="Border" Property="Background" Value="#444444"/>
                                                    </Trigger>
                                                </ControlTemplate.Triggers>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </ComboBox.Style>
                <ComboBoxItem Content="📷 Screenshot" Tag="0"/>
                <ComboBoxItem Content="Last 0:15" Tag="15"/>
                <ComboBoxItem Content="Last 0:30" Tag="30"/>
                <ComboBoxItem Content="Last 0:45" Tag="45"/>
                <ComboBoxItem Content="Last 1:00" Tag="60"/>
                <ComboBoxItem Content="Last 1:30" Tag="90"/>
                <ComboBoxItem Content="Last 2:00" Tag="120"/>
                <ComboBoxItem Content="Last 2:30" Tag="150"/>
                <ComboBoxItem Content="Last 3:00" Tag="180"/>
                <ComboBoxItem Content="Last 3:30" Tag="210"/>
                <ComboBoxItem Content="Last 4:00" Tag="240"/>
                <ComboBoxItem Content="Last 4:30" Tag="270"/>
                <ComboBoxItem Content="Last 5:00" Tag="300"/>
                <ComboBoxItem Content="Last 5:30" Tag="330"/>
                <ComboBoxItem Content="Last 6:00" Tag="360"/>
                <ComboBoxItem Content="Last 6:30" Tag="390"/>
                <ComboBoxItem Content="Last 7:00" Tag="420"/>
                <ComboBoxItem Content="Last 7:30" Tag="450"/>
            </ComboBox>
            
            <!-- Capture Button -->
            <Button x:Name="CaptureButton" Grid.Column="1" Content="📷" Width="44" Height="36"
                    Margin="0,0,4,0" IsEnabled="False" Click="CaptureButton_Click" ToolTip="Capture and send to AI">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Background" Value="#FF6B35"/>
                        <Setter Property="Foreground" Value="White"/>
                        <Setter Property="BorderThickness" Value="0"/>
                        <Setter Property="Cursor" Value="Hand"/>
                        <Setter Property="FontWeight" Value="SemiBold"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Button">
                                    <Border x:Name="bg" Background="{TemplateBinding Background}" CornerRadius="6">
                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="bg" Property="Background" Value="#E55A2B"/>
                                        </Trigger>
                                        <Trigger Property="IsEnabled" Value="False">
                                            <Setter TargetName="bg" Property="Background" Value="#444444"/>
                                            <Setter Property="Foreground" Value="#666666"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </Button.Style>
            </Button>
            
            <!-- New Chat Button -->
            <Button x:Name="NewChatButton" Grid.Column="2" Content="🔄" Width="44" Height="36"
                    IsEnabled="False" Click="NewChatButton_Click" ToolTip="Clear session and start fresh">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Background" Value="#333333"/>
                        <Setter Property="Foreground" Value="White"/>
                        <Setter Property="BorderThickness" Value="0"/>
                        <Setter Property="Cursor" Value="Hand"/>
                        <Setter Property="FontWeight" Value="SemiBold"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Button">
                                    <Border x:Name="bg" Background="{TemplateBinding Background}" CornerRadius="6">
                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="bg" Property="Background" Value="#444444"/>
                                        </Trigger>
                                        <Trigger Property="IsEnabled" Value="False">
                                            <Setter TargetName="bg" Property="Background" Value="#222222"/>
                                            <Setter Property="Foreground" Value="#555555"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </Button.Style>
            </Button>
        </Grid>
        
        <!-- Recording Status Indicator -->
        <Border x:Name="RecordingStatusBorder" Margin="0,6,0,0" 
                Background="#20FF0000" CornerRadius="4" Padding="8,4" Visibility="Collapsed">
            <StackPanel Orientation="Horizontal">
                <Ellipse x:Name="RecordingDot" Width="8" Height="8" Fill="#FF4444" Margin="0,0,6,0"/>
                <TextBlock x:Name="RecordingStatusText" Text="Recording: 0:00 / 7:30" 
                           Foreground="#FF8888" FontSize="10"/>
            </StackPanel>
        </Border>
        
        <!-- Image Status Indicator -->
        <Border x:Name="ImageStatusBorder" Margin="0,8,0,0" 
                Background="#20FFFFFF" CornerRadius="4" Padding="8,6" Visibility="Collapsed">
            <TextBlock x:Name="ImageStatusText" Text="📷 No image captured" 
                       Foreground="#888888" FontSize="11" TextWrapping="Wrap"/>
        </Border>
    </StackPanel>
</UserControl>









