<Window x:Class="Frontend.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:controls="clr-namespace:Frontend.Controls"
        Title="ERICAD Overlay"
        Height="800" Width="1200"
        WindowState="Maximized"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        Loaded="Window_Loaded"
        MouseRightButtonDown="Window_MouseRightButtonDown">

    <Grid x:Name="RootGrid">
        <!-- Layer 1: Main Canvas for positioning reference -->
        <Canvas x:Name="MainCanvas" Background="Transparent"/>
        
        <!-- Layer 2: Drawing Canvas - for ink strokes -->
        <Canvas x:Name="DrawingCanvas" 
                Background="Transparent"
                IsHitTestVisible="False"
                MouseLeftButtonDown="DrawingCanvas_MouseLeftButtonDown"
                MouseMove="DrawingCanvas_MouseMove"
                MouseLeftButtonUp="DrawingCanvas_MouseLeftButtonUp"/>
        
        <!-- Layer 3: Control Panel - ALWAYS on top and clickable -->
        <Canvas x:Name="PanelCanvas">
            <Border x:Name="ControlPanel"
                    Canvas.Right="20"
                    Canvas.Top="40"
                    MinWidth="340"
                    Background="#EE111111"
                    CornerRadius="12"
                    BorderBrush="#333333"
                    BorderThickness="1">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="25" ShadowDepth="0" Opacity="0.7" Color="Black"/>
                </Border.Effect>

                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- Header Control -->
                    <controls:HeaderControl x:Name="Header" Grid.Row="0"/>

                    <!-- Main Content (Collapsible) -->
                    <Grid x:Name="MainContent" Grid.Row="1" Margin="16,12">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <!-- Connection Controls -->
                        <controls:ConnectionControls x:Name="ConnectionCtrl" Grid.Row="0"/>
                        
                        <!-- Drawing Controls -->
                        <controls:DrawingControls x:Name="DrawingCtrl" Grid.Row="1" Margin="0,8,0,0"/>
                        
                        <!-- Capture Controls -->
                        <controls:CaptureControls x:Name="CaptureCtrl" Grid.Row="2" Margin="0,8,0,0"/>

                        <!-- Chat Panel -->
                        <controls:ChatPanel x:Name="ChatCtrl" Grid.Row="3" Margin="0,8,0,0"/>
                    </Grid>

                    <!-- Resize Grip -->
                    <Grid Grid.Row="2" Height="16">
                        <Border x:Name="ResizeGrip" 
                                HorizontalAlignment="Right" 
                                Width="16" Height="16" 
                                Margin="0,0,4,4"
                                Cursor="SizeNWSE"
                                Background="Transparent"
                                MouseLeftButtonDown="ResizeGrip_MouseLeftButtonDown"
                                MouseMove="ResizeGrip_MouseMove"
                                MouseLeftButtonUp="ResizeGrip_MouseLeftButtonUp">
                            <Path Data="M0,12 L12,0 M4,12 L12,4 M8,12 L12,8" 
                                  Stroke="#444444" StrokeThickness="1.5"/>
                        </Border>
                    </Grid>
                </Grid>
            </Border>
        </Canvas>
    </Grid>
</Window>
